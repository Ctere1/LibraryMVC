@model LibraryMVC.Models.admin
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Login</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <table align="center">
        <tr>
            <td>
                <div class="text-center">
                    <div class="container">
                        @ViewBag.Message
                        @using (Html.BeginForm("AdminLogin", "Auth", FormMethod.Post))
                        {
                            @("Admin Name")
                            <div class="form-group">
                                <input name="name" type="text" placeholder="Name" class="form-group" />
                            </div>
                            @("Password")
                            <div class="form-group">
                                <input name="password" type="password" placeholder="Password" class="form-group" />
                            </div>
                            <input type="submit" class="btn btn-primary" value="Login">
                        }
                    </div>
                    <br />
                    <div class="container" style="background-color:#f1f1f1">
                        <span class="psw">Login as <a href="#">@Html.ActionLink("user", "Login", "Auth")</a></span>
                    </div>
                </div>
            </td>
        </tr>
    </table>

    <div class="container body-content">
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Library</p>
        </footer>
    </div>

</body>
</html>

